{
  "name": "zaba-hra",
  "version": "1.0.0",
  "description": "Žába - hra, kde žába přeskakuje přes silnici. Postaveno na Capacitor pro Android APK.",
  "private": true,
  "scripts": {
    "start": "npx http-server www -p 8080",
    "sync": "npx cap sync android",
    "open:android": "npx cap open android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.2.0",
    "@capacitor/app": "^6.0.2",
    "@capacitor/core": "^6.2.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.2.0"
  }
}
